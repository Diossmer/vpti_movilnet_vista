const condicionesUtils=(params=null,newValue=null,result=null)=>{
  console.log("Traete el perfil y lo validas con una funcion.");
  console.log("Condiciones retornar True / false"+params,newValue,result);
  return {params,newValue,result};
};
export default condicionesUtils;
